import { DisplayListPipe } from './display-list.pipe';

describe('DisplayListPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayListPipe();
    expect(pipe).toBeTruthy();
  });
});
